version: "3.9"

services:
  opc-portal:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    env_file:
      - backend/.env
    environment:
      FLASK_ENV: production
    restart: unless-stopped